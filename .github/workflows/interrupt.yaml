name: Run Tests for Interrupt

on:
  workflow_call:
    secrets:
      cookie:
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  unwind:
    uses: ./.github/workflows/interrupt-unwind.yaml
    secrets:
      cookie: ${{ secrets.cookie }}
